# pt-ekklesia

The platform developer portal built on Backstage, running on GKE clusters managed by pt-pneuma. Provides a unified catalog of services, documentation, and self-service workflows for platform consumers.

This repo contains both the Backstage application (`app/`) and the OpenTofu deployment configuration (`deployments/`).
